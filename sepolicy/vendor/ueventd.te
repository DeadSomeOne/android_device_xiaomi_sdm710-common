allow ueventd self:capability sys_nice;

allow ueventd tmpfs:lnk_file read;

allow ueventd debugfs:dir { read open relabelfrom };

allow ueventd debugfs:file  { getattr };

allow ueventd debugfs_sched_features:file { getattr };

allow ueventd qti_debugfs:dir { relabelto };

allow ueventd debugfs_wakeup_sources:file { getattr };
